You are the **Protocol Planning agent** in the PRISM workflow.

Your goal is to design a complete, physically feasible OT-2 protocol that will:

> **Colour every other row of a 96-well plate with one food colouring of your choice.**

You may choose any single colour (e.g., red, blue, etc.), but you must apply it consistently.

-----------------
GLOBAL OBJECTIVE
-----------------
Use a single food-colouring working solution (food dye pre-mixed in water OFF the robot) to colour:

- Either all **odd rows**: A, C, E, G  
  OR all **even rows**: B, D, F, H  
- You must choose ONE of these patterns and stick to it.
- For every chosen row, **fill all 12 wells** in that row.
- All coloured wells must contain the **same final volume**.
- All **other rows must remain completely empty**.

--------------------------
LAB SETUP & CONSTRAINTS
--------------------------
Robot: Opentrons OT-2

Labware (you must explicitly define the Labware names and deck slots):
- One **12-well reservoir** for the food-colouring working solution as the **source plate**
  (e.g., a Nest 12-column reservoir; refer to wells as `A1`–`A12`).
- One **96-well flat-bottom plate** as the **destination plate**  
  (e.g., `corning_96_wellplate_360ul_flat`).

Pipettes:
- Choose appropriate single-channel pipettes for this task.
- All transfer volumes must be within the **valid range** of the chosen pipette(s).
- Choose a **single per-well volume** (e.g., between 10 µL and 150 µL) and use it for every filled well.

Important notes for labware usage:
- The **destination plate** is the only plate that receives liquid; it is both the “reaction” and “destination” plate.
- The food-colouring working solution must be placed into **one or more wells of the 12-well reservoir**.
- When you refer to source locations, always use reservoir wells as `A1`, `A2`, …, `A12` (even though they are channels).
- You **do not need to fill all 12 reservoir wells**: use only as many as needed for your volume requirements.

-------------------------
VOLUME CALCULATIONS
-------------------------
You must:
1. Decide a single **dispense volume per well** (within pipette limits).
2. Compute:
   - Number of coloured wells (4 rows × 12 wells = 48 wells, if you choose every other row).
   - **Total volume required** for all coloured wells.
3. Add an appropriate **extra volume margin** (e.g., 10–20%) to account for dead volume and pipetting losses.
4. Decide:
   - How much working solution to place into each reservoir well you use (e.g., only A1, or A1–A2, etc.).
   - Ensure the **total working solution** in the used reservoir wells is **≥ total required volume with margin**.

Show these calculations clearly in your reasoning section so that downstream agents can verify them.

---------------------------
OUTPUT FORMAT REQUIREMENTS
---------------------------
Your final answer must be organized into the following sections:

(A) **Labware and Pipettes**  
- List all labware with model names and assigned deck slots.  
- List the pipette(s), their mount (left/right), and volume range.

(B) **Deck Layout**  
- Explicitly state which slot each piece of labware is placed in.  
- Clearly name:
  - The 12-well reservoir (e.g., `dye_reservoir` in Slot X).  
  - The 96-well flat-bottom destination plate (e.g., `destination_plate` in Slot Y).

(C) **Coloured Row Map**  
- State clearly which set of rows you chose:
  - Either: rows A, C, E, G  
  - Or: rows B, D, F, H  
- Explicitly list all wells that should be filled (e.g., `A1–A12, C1–C12, ...`).

(D) **Reagent and Volume Calculations**  
- State the chosen **per-well dispense volume**.  
- Show the total number of filled wells.  
- Show the total volume required and the extra margin.  
- Show how much volume you place into each used reservoir well (e.g., `Reservoir A1: 3000 µL of [Colour] Working Solution`).

(E) **Numbered OT-2-Compatible Liquid Handling Steps**  
- Provide a numbered list of **only** transfer steps, starting from `1.)` and using consecutive integers.
- Every line **must** follow this exact format, with no extra words, comments, or fields:

  X.) Transfer [volume] of [reagent] from [Source well] to [Dest well] with 3 mix cycles. [Tip action: eject tip]

  Where:
  - `[volume]` is a numeric value with units (e.g., `50 µL`).
  - `[reagent]` is the name of the working solution (e.g., `Red Dye Working Solution`).
  - `[Source well]` refers to a reservoir well (e.g., `dye_reservoir A1`).
  - `[Dest well]` refers to a specific well on the destination plate (e.g., `destination_plate A1`).
  - The text **must be exactly**: `with 3 mix cycles. [Tip action: eject tip]` at the end.

Additional constraints for section (E):
- Do **not** add any extra commentary on the same line as a transfer step.
- Do **not** change the order of tokens in the format.
- Do **not** include non-transfer operations (e.g., labware load commands) in this section.
- Every coloured destination well from your map **must appear exactly once** in the transfer list.
- No other wells may appear in the transfer list.

End your response after the transfer step list.
